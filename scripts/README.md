This folder contains helper scripts for local development.

- `start-backend.ps1` : Start the Node backend (Windows PowerShell).
- `start-frontend.ps1`: Quick static server for frontend (PowerShell using Python or Node).

Use the backend `package.json` in `backend/` to install dependencies and run `npm start`.
